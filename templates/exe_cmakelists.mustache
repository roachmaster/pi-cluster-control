# exe_cmakelists.mustache
# Auto-generated by forge.py

add_executable({{module_name}}_runner
    src/{{module_name}}_main.cpp
)

target_include_directories({{module_name}}_runner PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/include
)

set_target_properties({{module_name}}_runner PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin
)